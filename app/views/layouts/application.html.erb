<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <% if user_signed_in? %>
      <%= link_to 'Выйти', destroy_user_session_path, method: :delete %>
      <%else %>
      <%= link_to 'Войти', new_user_session_path %>
    <% end %>
    <!-- <a href="/users/sign_in">Войти</a>|<a href="/users/sign_out" data-method="delete">Выйти</a> -->
    <!-- data-method="delete" так называемый data liks прописываем вручную на метод delete
     так как маршрут users/sign_out идет методом DELETE-->

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>
